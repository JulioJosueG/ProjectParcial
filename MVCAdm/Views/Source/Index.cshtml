@model IEnumerable<DataAccess.Source>

@{
    ViewData["Title"] = "Index";
}

<h1>Creacion y Modificacion - Sources</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.SourceName)
            </th>
            <th>
                State
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.SourceName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdStateNavigation.StateName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdSource">Edit</a> 
                
            </td>
        </tr>
}
    </tbody>
</table>
